from django.db import models
from django.contrib.auth.models import AbstractUser
from django.db import models
class User(AbstractUser):
    # إذا رغبت بإضافة خصائص جديدة للمستخدم لاحقًا، أضفها هنا
    pass


# Create your models here.
